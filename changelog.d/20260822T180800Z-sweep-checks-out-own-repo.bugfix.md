`pin-bump-sweep.yml` now checks out `robotsix-github-workflows` at `github.job_workflow_sha` instead of doing a bare checkout. As a reusable workflow, a bare `actions/checkout` puts the *caller's* repository in the workspace, so `python3 scripts/pin-bump.py` failed with "No such file or directory" — the first real execution of this workflow, since every scheduled run before it was skipped.
