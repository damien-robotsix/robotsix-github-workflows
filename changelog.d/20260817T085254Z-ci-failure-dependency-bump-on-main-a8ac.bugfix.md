Inline the `bump-setup` and `bump-commit-pr` composite-action steps into `deps-bump.yml`, `pin-bump.yml`, `pin-bump-sweep.yml`, and `auto-release.yml`, and delete the two composite actions. GitHub resolves relative `./` composite-action paths against the caller's checkout, so cross-repo callers (e.g. robotsix-mill) failed with "Can't find 'action.yml'" — inlining fixes all four reusable workflows for callers.
